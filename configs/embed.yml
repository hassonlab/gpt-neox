# Parameters used for generation of embeddings
# Make sure `load` is specified somewhere else
{
  # Don't use kv cache
  "recompute": true,

  # input/output file
  "sample-input-file": "sample_input.txt",
  "sample-output-file": "sample_embeddings.npy",
}
